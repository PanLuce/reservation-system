Creating Skupinka manually
1. when creating groups or lessons or anything, I want to have progress/waiting circle/bar visible - so the user knows that something is processing 
2. selecting color is not needed, should be based on the age (so it's always the same color per each age group) - just assign the colour per age group - use kids, soothing and calm colours 
3. poznámka is not necessary, moreover this field could be re-used for assigning Místo (location) which is really important thus required
4. current age groups are: 3-6 měsíců, 6-9 měsíců (do lezení), lezoucí děti, první krůčky - 1,5 roku, 1,5 - 2,5 roku, 2,5 - 4 roky, 1 - 2 roky, 2 - 3 roky, 1,5 - 3 roky

Managing Skupinky
1. When having Skupinka in the system and it's not full, it has to be possible to add the re a mom manually too (not just via excel import)
2. importing moms into skupinka has to be possible also when the skupinka already exists (adding only those moms that are NOT there yet)
3. importing moms into the system and creating skupinka in the process is done by using this example file tabulkaPRIHLASENI.ods
4. the example file is really critical point in the process. Propose the best way how to handle it as it has multiple worksheets, not really clear distinguishing between groups etc. Maybe 2-3 step uploading process?

notes: 
maminka@test.cz is not working for login
cleanup the DB, unnecessary tables

