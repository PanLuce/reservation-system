1. SKUPINKA can be uploaded via spreadsheet
2. lessons are at the first page/landing page
3. lessons are in more of a overview view, right now they are panels which is not scalable when having e.g. 9 upcoming lessons
4. I want to have seed data for customers/moms - test account even (just like we have on the login page the hint for admin credentials)
5. tests for the general flow of an admin (can be separated into units, ideally not e2e tests)
6. tests for the general flow of an customer (can be separated into units, ideally not e2e tests)
7. for moms there is landing page to view a calendar with each day, clickable button having a few information on it (icons, colour..), so a little bit more spacey
8. overview for moms also gives them quick look at the lessons they CAN submit based on their available substitutions (which are meant for specific age limit, not location)

Creating Skupinka manually
1. when creating groups or lessons or anything, I want to have progress/waiting circle/bar visible - so the user knows that something is processing 
2. selecting color is not needed, should be based on the age (so it's always the same color per each age group) - just assign the colour per age group - use kids, soothing and calm colours 
3. poznámka is not necessary, moreover this field could be re-used for assigning Místo (location) which is really important thus required
4. current age groups are: 3-6 měsíců, 6-9 měsíců (do lezení), lezoucí děti, první krůčky - 1,5 roku, 1,5 - 2,5 roku, 2,5 - 4 roky, 1 - 2 roky, 2 - 3 roky, 1,5 - 3 roky

Managing Skupinky
1. When having Skupinka in the system and it's not full, it has to be possible to add the re a mom manually too (not just via excel import)
