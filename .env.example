# Environment Configuration for Production Deployment

# Node Environment (set to 'production' for production deployment)
NODE_ENV=production

# Session Secret (IMPORTANT: Change this to a random string in production!)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-secret-key-here-change-in-production

# Allowed Origins for CORS (comma-separated list of domains that can embed the app)
# Include your WordPress site domain(s)
ALLOWED_ORIGINS=https://centrumrubacek.cz,https://www.centrumrubacek.cz

# Server Port (optional, defaults to 3000)
PORT=3000
